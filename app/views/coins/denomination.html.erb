<body>
    <div>
        <%= render "shared/nav_bar" %>
    </div>
    <div>
        <ul>
            <% @coins.each do |coin| %>
        <li>
            <%="#{coin.denomination}, this coin is on offer for #{coin.price}, the coins condition is rated #{coin.condition}, and was minted in #{coin.mint_year}"%>          
        <% end %>
    <div>
        <%= render "shared/footer" %>
    </div>
</body>
 


    

